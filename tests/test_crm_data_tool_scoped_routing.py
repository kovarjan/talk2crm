from __future__ import annotations

# Tests for scope-based routing via crm_data_tool have been removed.
# crm_data_tool no longer exists; crm_query_tool does not accept a scope
# parameter — the LLM specifies the module directly.
